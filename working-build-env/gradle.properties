# Done to increase the memory available to Gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod properties
mod_version = 0.8.0
maven_group = com.github.crittscott.somebuckets
archives_name = somebuckets
enabled_platforms = fabric,forge

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id = somebuckets
# The human-readable display name for the mod.
mod_name = Some Buckets
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license = GPL-3.0
# The authors of the mod. This is a simple text string that is used for display purposes in the mod list.
mod_authors = crittscott
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
mod_description = Get you some buckets!

# Minecraft properties
minecraft_version = 1.20.1
# The Minecraft version range can use any release version of Minecraft as bounds.
minecraft_version_range = [1.20.1]
# The Forge and loader ranges are runtime compatibility declarations for mods.toml.
# Some Buckets supports recommended Forge 47.4.10 and newer 47.x releases for Minecraft 1.20.1.
forge_version_range = [47.4.10,48)
# The loader version range can only use the major version of Forge/FML as bounds
loader_version_range = [47,48)
# Mojang official mappings layered with Parchment. See https://parchmentmc.org/docs/getting-started
parchment_minecraft = 1.20.1
parchment_version = 2023.09.03

# Dependencies
fabric_loader_version = 0.19.3
fabric_api_version = 0.92.11+1.20.1
# Gradle needs one exact Forge artifact for compilation and development runs. Keep it aligned with
# the minimum supported runtime declared by forge_version_range above.
forge_compile_version = 1.20.1-47.4.10
ftb_chunks_version = 2001.3.8
